I"š(function() {
  $(document).on('turbolinks:load', function() {
    return $(document).on('click', '#likes', function() {
      var ans_id, countl, likes;
      countl = $("#show_likes").text();
      countl = parseInt(countl);
      ans_id = $(this).val();
      likes = $(this).outerHTML;
      likes = parseInt(likes);
      $.ajax('/comments/update', {
        type: 'PATCH',
        dataType: 'text',
        data: {
          ans_id: ans_id
        },
        success: function(data) {
          $(this).prop("disabled", true);
          return $("#show_likes").text(countl + 1);
        }
      });
      return false;
    });
  });

  return;

}).call(this);
:ET